package com.example.eventuate.orset


object ORSetBotA extends App {

 /* val locationA = new EventuateLocation("location-A")
  val f = locationA.orSetService.remove("orSet-1","Gabriel")
  f onSuccess {
    case x => println(x)
  }
  Await.result(f, 2 minutes)
  */
}
/*

object ORSetBotB extends App {

  val location = new EventuateLocation("location-B")
  val f = location.orSetService.add("orSet-1","Gabriel")
  f onSuccess {
    case x => println(x)
  }
  Await.result(f, 2 minutes)
}

object ORSetClientBotB extends App {
  val locationA = new EventuateLocation("location-B")
  val f = locationA.orSetService.value("orSet-1")
  f onSuccess {
    case x => println(x)
  }
  Await.result(f, 2 minutes)
}
*/